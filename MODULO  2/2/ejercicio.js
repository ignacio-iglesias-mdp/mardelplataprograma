
//mayor y menor

var n1,n2,n3,t,mayor;
	n1 = parseInt(prompt("Número 10"));
	n2 = parseInt(prompt("Número 25"));
	n3 = parseInt(prompt("Número 03"));

	if (n1 > n2) {
		t = n1;
	}else{
		t = n2;
	}

	if (t > n3) {
		mayor = t;
	}else{
		mayor = n3;
	}

    console.log("El mayor es "+mayor);


//Par o Impar
const numero = 2;
const esPar = (num) => % 2 === 0;
const esImpar = (num) => % 2 === 1;

      // el numero es Par?
console.log(
    'El ${numero} PAR: ${esPar(numero)}'
)
      // el numero es Impar?
console.log(
    'El ${numero} IMPAR: ${esImpar(numero)}'
)

// Dos variables varias operaciones





